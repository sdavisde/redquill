Task 4.0 smoke transcript — async remote ops & command log
Driver: real BackgroundTasks + ui::dispatch_key (the handlers
the blocking event loop calls). tmux unavailable; headless.
Wall-clock start (unix seconds): 1783773888

== Part (a): render loop stays responsive during a slow op ==
A 2s `sleep` runs on the real background poller while j/k drive
the diff cursor through dispatch_key. Each keypress is timed from
op-spawn; all land in single-digit ms, far under the 2000ms op.

t=+   0ms spawned slow op (remote_op=Some(Fetch), running_label=Some("fetch"))
t=+   0ms press j: diff_cursor 0->1 | op still pending=true log_len=0
t=+   0ms press j: diff_cursor 1->2 | op still pending=true log_len=0
t=+   0ms press j: diff_cursor 2->3 | op still pending=true log_len=0
t=+   0ms press k: diff_cursor 3->2 | op still pending=true log_len=0

Observation: all 4 keypresses processed by t=+0ms while the
2000ms op was still pending — dispatch never blocked on it.
t=+2041ms op completed and drained (log_len=1)

== Part (b): a non-fast-forward push rejection is logged ==
A file:// bare remote is advanced by a second clone; the local
clone commits too, so `git push` is rejected non-fast-forward.
Driven through App::request_remote_op -> poll_remote (real spawn).

spawned push (running_label=Some("push"))
logged: command_line="git push" exit_status="exit 1" success=false
stderr (verbatim from git):
    To file:///var/folders/4_/h69pjmp54bngbb8vfn2_j5mw0000gn/T/.tmpi0zyvE
     ! [rejected]        main -> main (fetch first)
    error: failed to push some refs to 'file:///var/folders/4_/h69pjmp54bngbb8vfn2_j5mw0000gn/T/.tmpi0zyvE'
    hint: Updates were rejected because the remote contains work that you do not
    hint: have locally. This is usually caused by another repository pushing to
    hint: the same ref. If you want to integrate the remote changes, use
    hint: 'git pull' before pushing again.
    hint: See the 'Note about fast-forwards' in 'git push --help' for details.

command-log pane visible: true | shows rejection text: true
Observation: the rejected push did not crash the tool; it landed
in the command log with its nonzero exit and git's own stderr.
